input_dir: "data/input"
output_dir: "data/output"

files:
  - "audio.wav"   

pipeline:
  - name: "bandpass"
    low_cutoff: 1000
    high_cutoff: 4000

  - name: "mixnoise"
    snr_db: 50

  - name: "pitchshift"
    semitones: -2

output_formats:
  - "wav"
  - "mp3"
